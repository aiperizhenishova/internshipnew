spring:
  datasource:
    url: jdbc:postgresql://junction.proxy.rlwy.net:39547/railway  # URL из DATABASE_PUBLIC_URL
    username: postgres  # Имя пользователя из переменных
    password: UperfiPnBxRHASJrBkoRTHGMEkVqPuCJ  # Пароль из переменных

  jpa:
    hibernate:
      ddl-auto: update  # Устанавливает автогенерацию схемы (можно использовать: none, validate, update, create, create-drop)
    show-sql: true  # Показывать SQL-запросы в логах

  server:
    port: 8080

  main:
    allow-circular-references: true


  mail:
    host: smtp.gmail.com
    port: 587
    username: zhenishovaaiperi@gmail.com
    password: Aiperi22
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true